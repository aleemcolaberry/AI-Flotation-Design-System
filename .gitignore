# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/

# Tooling — there is no build step; @babel/standalone is only installed ad hoc to rerun app/build.js
node_modules/
dist/
.cache/
npm-debug.log*
package-lock.json

# Raw Claude Design downloads dropped here for a re-sync (the unbundled files are the repo; the export stays local)
AI Flotation Design System*.html
